Step:1  Activate virtual enviroment
Step:2   Install required module from requirements.txt(pip install -r requirements.txt)
Step:3   Dump the .sql file in mysql database(i am using my sql yog tool, so directly dump into the mysql)
Step:4 	 Run the app.py 
Step:5 	 All the api is available in the API(excel) file(Just fallow it and you can see the resposne also)
